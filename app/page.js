import {Nav} from './nav';import {getStandings,getTeamDirectory} from '../lib/data';export const dynamic='force-dynamic';
export default async function Home(){const [standings,teams]=await Promise.all([getStandings(),getTeamDirectory()]);return <main className="shell"><div className="topbar"><div><div className="brand">CFB Team Fantasy</div><div className="sub">2026 League Dashboard</div></div><div className="pill">8 Owners · 88 Rostered Teams</div></div><Nav/><section className="section"><div className="grid"><div className="card"><div className="muted">Current Leader</div><div className="kpi">{standings[0]?.owner_name||'—'}</div><div>{standings[0]?.fantasy_points??0} pts</div></div><div className="card"><div className="muted">Free Agents</div><div className="kpi">{teams.filter(t=>!t.is_owned).length}</div><div>Across all FBS conferences</div></div><div className="card"><div className="muted">Live Data</div><div className="kpi">Next</div><div>CFBD connection is the next phase</div></div></div></section><section className="section"><div className="sectionTitle"><h2>Standings</h2><span className="muted">Season totals</span></div><div className="tableWrap"><table className="table"><thead><tr><th>Rank</th><th>Owner</th><th>Draft Slot</th><th>Points</th></tr></thead><tbody>{standings.map(s=><tr key={s.owner_id}><td>{s.rank}</td><td><b>{s.owner_name}</b></td><td>{s.draft_slot}</td><td>{s.fantasy_points}</td></tr>)}</tbody></table></div></section></main>}
