import {NextResponse} from 'next/server';import {isAdminAuthenticated} from '../../../../lib/admin-auth';import {adminRpc} from '../../../../lib/admin-rpc';export async function GET(){if(!await isAdminAuthenticated())return NextResponse.json({ok:false},{status:401});try{const [sync,audit]=await Promise.all([adminRpc('admin_system_health'),adminRpc('admin_get_audit')]);return NextResponse.json({ok:true,health:sync,audit});}catch(e){return NextResponse.json({ok:false,error:e?.message||String(e)},{status:500})}}