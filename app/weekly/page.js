import {Nav} from '../nav';import {getWeekly} from '../../lib/data';export const dynamic='force-dynamic';export default async function Page(){const rows=await getWeekly();return <main className="shell"><div className="topbar"><div><div className="brand">Weekly</div><div className="sub">Weekly prize race and scoring history</div></div></div><Nav/><div className="card section">{rows.length?<div className="tableWrap"><table className="table"><thead><tr><th>Week</th><th>Owner</th><th>Points</th></tr></thead><tbody>{rows.map((r,i)=><tr key={i}><td>{r.week_key}</td><td>{r.owner_name}</td><td>{r.weekly_points}</td></tr>)}</tbody></table></div>:<div className="liveEmpty">Weekly scoring will populate automatically once games begin.</div>}</div></main>}
