import {Nav} from '../nav';import {LeagueHeader} from '../league-header';import {TeamName} from '../team-name';import {getStandings,getTeamDirectory,getLatestTeamProjections} from '../../lib/data';export const dynamic='force-dynamic';
export default async function Page(){const [standings,teams,projections]=await Promise.all([getStandings(),getTeamDirectory(),getLatestTeamProjections()]);const pm=new Map(projections.map(p=>[Number(p.team_id),p.projected_points]));return <main className="shell"><LeagueHeader/><Nav/><section className="section"><div className="sectionTitle"><h2>Rosters</h2><span className="muted">All owners</span></div><div className="leagueRosterGrid">{standings.map(o=><details className="card leagueRosterCard" key={o.owner_id}><summary><b>{o.owner_name}</b><span>Rank #{o.rank??'—'} · {o.fantasy_points??0} pts</span></summary><div className="leagueRosterTeams">{teams.filter(t=>t.is_owned&&Number(t.owner_id)===Number(o.owner_id)).sort((a,b)=>a.conference_display_order-b.conference_display_order).map(t=><div key={t.team_id}><span><TeamName team={t} size="sm"/><small>{t.conference_code}</small></span><b>{pm.has(Number(t.team_id))?Number(pm.get(Number(t.team_id))).toFixed(1):'—'}</b></div>)}</div></details>)}</div></section></main>}
