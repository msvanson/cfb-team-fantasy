'use client';
export default function TeamProjectionChart({history=[]}){if(!history.length)return <div className="card standingsChartEmpty">Projection history will appear after the preseason projection is published.</div>;const raw=history.map(r=>({...r,value:Number(r.projected_points),wins:Number(r.projected_wins)})).filter(x=>Number.isFinite(x.value));const byRun=[];for(const r of raw){if(!byRun.length||byRun.at(-1).run_id!==r.run_id)byRun.push(r);else byRun[byRun.length-1]=r}const points=byRun.map((r,i)=>({...r,i,label:i===0?'Pre':`W${i}`}));if(!points.length)return <div className="card standingsChartEmpty">No projection history available.</div>;const W=680,H=270,L=48,R=24,T=24,B=52,vals=points.map(x=>x.value),lo=Math.floor(Math.min(...vals)-1),hi=Math.ceil(Math.max(...vals)+1),span=Math.max(1,hi-lo),x=i=>points.length===1?(L+W-R)/2:L+i*(W-L-R)/(points.length-1),y=v=>T+(hi-v)*(H-T-B)/span;return <div className="card teamProjectionCard"><div className="teamProjectionScroll"><svg viewBox={`0 0 ${W} ${H}`} className="teamProjectionSvg" role="img" aria-label="Season projected points history">{[lo,lo+span/2,hi].map((v,i)=><g key={i}><line x1={L} x2={W-R} y1={y(v)} y2={y(v)} className="rankGrid"/><text x={L-8} y={y(v)+4} textAnchor="end" className="rankAxis">{v.toFixed(1)}</text></g>)}{points.length>1?<polyline points={points.map(p=>`${x(p.i)},${y(p.value)}`).join(' ')} fill="none" className="teamProjectionLine"/>:null}{points.map(p=><g key={p.i}><circle cx={x(p.i)} cy={y(p.value)} r="5" className="teamProjectionDot"/><text x={x(p.i)} y={H-22} textAnchor="middle" className="weekAxis">{p.label}</text><title>{`${p.label}: ${p.value.toFixed(2)} projected pts`}</title></g>)}</svg></div><div className="teamProjectionLatest"><span><small>Current projection</small><b>{points.at(-1).value.toFixed(2)} pts</b></span><span><small>Projected wins</small><b>{points.at(-1).wins.toFixed(2)}</b></span><span><small>Change</small><b>{points.length>1?`${points.at(-1).value-points[0].value>=0?'+':''}${(points.at(-1).value-points[0].value).toFixed(2)}`:'—'}</b></span></div></div>}
