import {Nav} from '../nav';import {getWeekly,getStandings,getTeamDirectory} from '../../lib/data';export const dynamic='force-dynamic';

function weekOrder(k){if(!k)return 999;const m=String(k).match(/Week\\s+(\\d+)/i);if(m)return Number(m[1]);if(k==='Conference Championships')return 100;if(k==='CFP')return 101;if(k==='National Championship')return 102;if(k==='Postseason')return 103;return 999}

export default async function History(){const [weekly,standings,teams]=await Promise.all([getWeekly(),getStandings(),getTeamDirectory()]);
const grouped=weekly.reduce((a,r)=>((a[r.week_key]??=[]).push(r),a),{});
const weeklyWinners=Object.entries(grouped).map(([week,rows])=>{const max=Math.max(...rows.map(r=>r.weekly_points));const winners=rows.filter(r=>r.weekly_points===max);return {week,points:max,winners:winners.map(r=>r.owner_name)}}).sort((a,b)=>weekOrder(a.week)-weekOrder(b.week));
const leader=standings[0];const topTeam=[...teams].sort((a,b)=>b.fantasy_points-a.fantasy_points||b.wins-a.wins)[0];
return <main className="shell"><div className="topbar"><div><div className="brand">History</div><div className="sub">League archive, weekly winners and season records</div></div></div><Nav/>
<section className="section grid"><div className="card"><div className="muted">2026 Current Leader</div><div className="kpi">{leader?.owner_name||'—'}</div><div>{leader?.fantasy_points??0} pts</div></div><div className="card"><div className="muted">Top Scoring Team</div><div className="kpi historyTeam">{topTeam?.school||'—'}</div><div>{topTeam?.fantasy_points??0} pts</div></div><div className="card"><div className="muted">Completed Seasons</div><div className="kpi">0</div><div>2026 is the inaugural season</div></div></section>
<section className="section"><div className="sectionTitle"><h2>Weekly Winners</h2><span className="muted">Permanent weekly prize archive</span></div>{weeklyWinners.length?<div className="tableWrap"><table className="table"><thead><tr><th>Week</th><th>Winner</th><th>Points</th></tr></thead><tbody>{weeklyWinners.map(w=><tr key={w.week}><td>{w.week}</td><td><b>{w.winners.join(' / ')}</b></td><td>{w.points}</td></tr>)}</tbody></table></div>:<div className="card liveEmpty">Weekly winners will appear here automatically once the season begins.</div>}</section>
<section className="section"><div className="sectionTitle"><h2>Season Champions</h2><span className="muted">All-time league results</span></div><div className="card liveEmpty">The first champion will be added when the 2026 season is complete.</div></section>
<section className="section"><div className="sectionTitle"><h2>Record Book</h2><span className="muted">Grows with each season</span></div><div className="historyRecords"><div className="card"><b>Highest Season Score</b><div className="muted">Established after 2026</div></div><div className="card"><b>Highest Weekly Score</b><div className="muted">Tracks automatically once games begin</div></div><div className="card"><b>Most Weekly Wins</b><div className="muted">Tracks automatically through the season</div></div><div className="card"><b>Best Individual Team Season</b><div className="muted">Tracks fantasy points by FBS team</div></div></div></section>
</main>}