import { Nav } from '../nav';import { getTeamDirectory } from '../../lib/data';import { isAdminAuthenticated } from '../../lib/admin-auth';import { Login,AdminPanel } from './admin-client';export const dynamic='force-dynamic';
export default async function Admin(){const authed=await isAdminAuthenticated();const teams=authed?await getTeamDirectory():[];return <main className="shell"><div className="topbar"><div><div className="brand">Commissioner</div><div className="sub">Private league administration</div></div></div><Nav/><section className="section">{authed?<AdminPanel teams={teams}/>:<Login/>}</section></main>}
